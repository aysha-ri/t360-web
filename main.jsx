import React from "react";
import { createRoot } from "react-dom/client";
import App from "./App.jsx";

// Mount the React app at the #root element
createRoot(document.getElementById("root")).render(<App />);
